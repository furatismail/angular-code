import { PasswordStrDirective } from './password-str.directive';

describe('PasswordStrDirective', () => {
  it('should create an instance', () => {
    const directive = new PasswordStrDirective();
    expect(directive).toBeTruthy();
  });
});
